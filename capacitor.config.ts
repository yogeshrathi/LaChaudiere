import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'la-chaudiere',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
